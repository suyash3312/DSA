-> InOrder Traversal Of A BST is always Sorted.
-> To find the Lowest Commom Ancestor of A Binary Search Tree, we can find LCA using the code used in Binary Tree
but avoid using that code bcoz it has more time complexity.